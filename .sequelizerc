const path = require('path');

module.exports = {
  'config': path.resolve('./src/app/config', 'config.json'),
  'migrations-path': path.resolve('./src/app', 'migrations'),
  'models-path': path.resolve('./src/app', 'models'),
  'seeders-path': path.resolve('./src/app', 'seeders')
}

// chạy câu lệnh sau để thực thi:  node_modules/.bin/sequelize init
// node_modules/.bin/sequelize model:create --name User --attributes "email:string"
// run: node_modules/.bin/sequelize db:migrate - map vào database

//  npm install -g sequelize-cli   
// npm install sequelize           